# Security: enforce HTTPS registry only
registry=https://registry.npmjs.org/

# Prevent unexpected lifecycle scripts from running
# Whitelist specific packages via pnpm-workspace.yaml onlyBuiltDependencies
ignore-scripts=false

# Strict SSL checking
strict-ssl=true

# Auto-install peer dependencies (pnpm default)
auto-install-peers=true
